"""Integration tests for EmailReader project.

Integration tests that verify end-to-end workflows using real external services.
These tests require valid credentials and are skipped by default.

Run with: pytest tests/integration/ --run-integration -v
"""
